# WIP — 交接文件（更新 2026-07-08）

> 給下一個接手的 AI／未來 session：本文件描述《天下布武》規格的當前進度與剩餘工作。
> 讀完本文件 + `plan/00-foundations.md` + `plan/19-glossary.md §3.13` 即可無縫接續。

## 專案目標

仿《信長之野望·新生》的網頁單機遊戲（工作名《天下布武》），UI 全繁體中文。
目前階段**只寫規格**（`plan/` 目錄，21 份文件），詳細到讓 Claude Opus 4.8 直接照著實作、不需再做設計決策。

---

## ✅ 第 1 步完成：`plan/15-balance.md`（1,123 行）

- 涵蓋全部 **631** 個 BAL 常數：536 模擬層主表（§5.1，依擁有文件分 15 子表）＋ 35 非模擬（表 D，不進 balance.ts）＋ 60 廢棄別名併入（表 B）。
- 裁決：15 項同名異值衝突（表 A）、60 項同義/重名別名、8 項機制重疊（表 C）；四條平衡例算全達標（§5.3）。
- **命名一律以 `19-glossary.md` §3.13 為準**（本文件只定「值」），已與其對齊。
- 可重跑工具：`scratchpad/bal-catalog.json`（631 常數結構化）＋ `scratchpad/gen-master-table.js`（確定論主表生成器）。

## ✅ 第 2 步完成：六視角驗證 → 全部彙整進 `19 §3.13`（E-01…E-80）

- 六視角 workflow（find→對抗式覆核）確認 31 項；另發現設計者原有勘誤表 **E-01…E-56**，把新確認者補為 **E-57…E-80**。
- **`19 §3.13` E-01…E-80 = 唯一、完整、權威的跨文件修正台帳**（每條含 衝突／涉及文件／建議定案）。BAL 細節見 `15 §5.2`。

## 🔧 第 3 步（修正）：約 90% 完成並 commit；剩 ~11 條互相牽連的結構性項

**做法**：先主迴圈手動套 ~19 條安全項，後以「一檔一 agent」workflow 一次套 16 檔約 93 條，每檔於 §8 補消化記錄。
**已全部 git commit**（見 `git log`；首版 + 7 個 fix commit）。
**已驗證**：無簡體/日文新字體（僅 13/14/17/19 之刻意示例/黑名單/測試字串）、無截斷、無重複新增欄位、
合戰模型 E-18 已置換為 07 陣節點圖、02/08/15 官位序三方一致。

**剩餘 ~11 條（agent 標記 needsReview；互相牽連，須成組處理、每組一 commit，勿單點硬改）**：
- **02 側收尾**（他檔已改、02 未收）：`ai:AiState` 分支收錄(E-60，09 已改型別)、朝廷獻金機制重寫 `CmdDonateCourt`→`CmdStartDiploWork(target:'court')`(E-27)、Army 行軍欄位模型(E-11，須先定 E-36 baseDays 模型)。
- **Command/GameEvent 聯集殘留**（待 02 §4.18/§4.19 完成 E-32 後回寫）：03 `actorClanId`→`clanId`/去 `cmd.` 前綴(E-29)、03 `military.armyArrived` 事件(E-30)、05 `buildFacility` 參數 `facilityId`→`facilityTypeId`(E-29)、06 具申指令併入 02 聯集(E-29)、07 Army 欄位改名 `generalId`→`leaderId` 等(E-11)。
- **08 外交大改**（跨全檔、推翻 D1/D10，最大宗）：`PactKind` 改 00 §14 四協定/婚姻獨立 kind/不可侵降 v1.1(E-23)、外交資料結構改依 02 DDR-3 稀疏 rows(E-28)。動 02＋08＋09，建議最後做。
- **小尾**：09 領主開發治安副作用確認(E-07)、12/11 協定 icon 二擇一(E-76)、13 調略字串待 11 plot panel 定案(E-66)、16 uiScale 分類(E-56 nit)、17 掃描器 L1 超集 vs 雙向相等(E-73 nit)。

**續作方法**：
1. 逐檔套用 `19 §3.13` 中「涉及文件」含該檔者；BAL 名/值以 `15 §5.2` 為準；一檔（或一組連動檔）一 commit。
2. 或分批重跑修正 workflow：`Workflow({scriptPath: ".../workflows/scripts/apply-errata-wf_0c608cfc-23b.js", args: ["05","06"]})`。
   ⚠ 注意：`args` 需為真陣列，若被序列化成字串則會**套全部 16 檔**（本次即因此意外一次全套）；該腳本已移除 worktree isolation、直接改真檔。

---

## 完成後待辦（Step 3 全部收尾後）

1. 抽查 3~4 份文件確認修正品質（尤其 02 樞紐、08 外交、07 合戰）。
2. E-01…E-80 全數消化後，**才刪除本 `wip.md`**。
3. 開始實作：用 Opus 4.8 從 `plan/18-roadmap.md` M0 起；開工前先讀 `plan/README.md` + `plan/00-foundations.md`。

## 關鍵事實（給接手者）

- **不得修改 `plan/00-foundations.md`**（最高準則）。規格衝突優先序：`00 > 02 > 15 > 系統文件(03~10,16) > UI 文件(11~13)`；BAL 值以 15 為準、名以 19 §3.13 為準。
- 21 份 plan 文件全部完成並已 commit。持久記憶 `tenka-fubu-project.md` 已更新至現況。
- 使用者額度多次用盡（5 小時窗口）；接手時如又撞限，把進度更新回本文件即可。
